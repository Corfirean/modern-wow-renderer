; Modern WoW Renderer runtime switches.
; F11 toggles the complete graphics package, F7 opens tuning, F12 reloads
; GraphicsEffects.ini. Visual tuning lives in GraphicsEffects.ini.
[Graphics]
UnifiedToggle=1
Enabled=1
ShowStatus=0

[Atmosphere]
; The old flat overlay is disabled; the volume pass below replaces it.
Enabled=0
FogOpacity=42
FogHeight=0.58
FogBlue=145
FogGreen=118
FogRed=86
RaysOpacity=36
SunX=0.72
SunY=0.18

[Diagnostics]
Enabled=0
AutoCapture=0
MaxDraws=2048
SkipDraws=0

; F9 captures every draw call for ONE frame to CelestialDiagnostics/<timestamp>/
; celestial_candidates.txt, flagging small/alpha-blended/no-zwrite draws as
; CANDIDATE=1 (a filter to make the log readable, not a verdict) and dumping
; VS/PS bytecode for those. Off by default; read-only, no rendering changes.
[CelestialDiagnostics]
Enabled=0

[Highlight]
Enabled=0
Group1PixelShader=ebecd65635a1a75e

[Water]
Enabled=1
ShowStatus=0

[DistanceFog]
; The game's OWN native distance fog (rescales its real per-shader fog
; constants). Independent of the atmosphere pass - this is what actually
; guarantees the render/streaming distance edge stays covered.
Enabled=1
ShowStatus=0

[Volume]
Enabled=1
